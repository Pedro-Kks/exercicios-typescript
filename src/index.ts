import { olaMundo, resultado, nomeInteiro } from "./exer";

(function main(): void {
    console.log(olaMundo());
    console.log(`O resultado da multiplicação é: ${resultado}.`);
    console.log(`O nome completo da pessoa é: ${nomeInteiro}.`);
})();
